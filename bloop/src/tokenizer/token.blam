pub enum Type {
    SKIP,
    RAW,
    ...,
    MULT
}

pub struct Token {
    Type type
    u16 pos
    u16 len
    Token prev
    Token next
}

pub const Map<Type, str> TOKEN_EXPR = {
    {Type::MLINE_COMMENT, R"(#\*[^*]*\*#)"},
    ...,
    {Type::MULT, R"(\*)"}
}
