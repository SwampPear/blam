# Bloop



```
bloop <file>
```

## Install Package

```
bloop install <package>
```

## Build

Builds from file, fails if no main found in file. Creates a blam.json file. 
Creates build dir.

```
bloop build <file>
```