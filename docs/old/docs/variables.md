# Variables

Variables can be assigned with or without types.

```
// without type
a = 10

// with type
i8 a = 11
```

Variables are mutable by default but can be declared immutable with
the const keyword.

```
const a = 10
const i8 a = 10
```